<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:map="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF">

    <Style  TargetType="{x:Type map:Pushpin}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type map:Pushpin}">
                    <Grid>
                        <!-- Custom pushpin design -->
                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="3" CornerRadius="50" Height="22" Width="22">
                            
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"  Text="{TemplateBinding Content}" />
                        </Border>
                        <!-- Content presenter -->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>